#for this we need to add s3 full acess to IAM user

resource "aws_s3_bucket" "mybucket" {
  bucket = "geeths3"
}

resource "aws_s3_bucket_accelerate_configuration" "example" {
  bucket = aws_s3_bucket.mybucket.bucket
  status = "Enabled"
}
